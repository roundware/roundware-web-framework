sudo: false
language: node_js
node_js:
  - "iojs"
  - "7"
cache:
  directories:
    - node_modules
branches:
  only:
    - master
notifications:
  email: false
before_script:
  - npm prune
script:
  #- npm run cover
  #- npm run check-coverage # does not work yet
  - npm test
  - npm run build
after_success:
  #- npm run report-coverage # TODO figure out nice way to report this; codecov.io wanted way too many permissions
